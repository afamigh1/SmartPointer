The test.cpp file is the test file for the SharedPtr with a single abort commented out
because there is a small leak on the first set of tests. Also the threads leak a lot of
bytes and I couldn't figure out why.
